# Copy this file to `.env.local` for local dev or set on Vercel as env vars

# Base URL for Axios requests (should include /api if your backend prefixes routes with /api)
VITE_API_URL=http://localhost:3000/api

# Base URL for Socket.IO connection (no /api suffix)
VITE_SOCKET_URL=http://localhost:3000
# Frontend envs for Vite
# If hosting frontend and backend on the same domain (Render single service), leave these empty.
# If hosting separately (e.g., Netlify/Vercel for frontend, Render/Railway for backend), set full URLs.

# API base URL for axios, e.g., https://your-backend.example.com/api
VITE_API_URL=

# Socket server base URL (no trailing slash), e.g., https://your-backend.example.com
VITE_SOCKET_URL=
